# Only `npm run spike` (model comparison script) reads this file.
# The app itself takes the key from its Settings page and keeps it in the browser.
OPENROUTER_API_KEY=

# `NEXT_PUBLIC_MOCK_API=1 npm run dev` runs the UI against fixtures/analysis.json, no key needed.
# NEXT_PUBLIC_MOCK_API=1
